-- Khởi tạo Rayfield Library
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Bounty Hunter V13 - tyhugVN",
   LoadingTitle = "Đang nạp hệ thống Aimbot & FOV...",
   LoadingSubtitle = "by tyhugVN",
})

-- =========================
-- 🛠 BIẾN HỆ THỐNG
-- =========================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local VirtualUser = game:GetService("VirtualUser")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local Camera = workspace.CurrentCamera
local player = Players.LocalPlayer

-- Chiến đấu & Di chuyển
local killAura = false
local autoChat = false
local autoEquip = false
local fakeDeath = false
local speedEnabled = false
local walkSpeedValue = 16
local jumpEnabled = false
local jumpPowerValue = 50

-- Aimbot & FOV
local aimbotEnabled = false
local aimOnHold = false
local autoLock = false
local fovRadius = 100
local fovSides = 60
local fovThickness = 1
local fovTransparency = 1
local fovColor = Color3.fromRGB(255, 255, 0)
local isHoldingAim = false

-- ESP
local espMaster = false
local showBox = false
local showName = false
local showLine = false
local showHealth = false
local showDistance = false
local showCount = false

-- Spectate & Items
local spectateEnabled = false
local selectedSpectatePlayer = nil
local selectedWeapon = nil
local currentTarget = nil

-- Drawing Objects
local espObjects = {}
local playerCountLabel = Drawing.new("Text")
playerCountLabel.Visible = false
playerCountLabel.Color = Color3.fromRGB(255, 255, 0)
playerCountLabel.Size = 20
playerCountLabel.Outline = true
playerCountLabel.Position = Vector2.new(50, 80)

local crosshairVertical = Drawing.new("Line")
local crosshairHorizontal = Drawing.new("Line")
local crosshairEnabled = false

local fovCircle = Drawing.new("Circle")
fovCircle.Visible = false

-- =========================
-- 🔍 HÀM HỖ TRỢ
-- =========================

local function updateFOV()
    fovCircle.Radius = fovRadius
    fovCircle.Thickness = fovThickness
    fovCircle.Transparency = fovTransparency
    fovCircle.Color = fovColor
    fovCircle.NumSides = fovSides
    fovCircle.Position = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)
    fovCircle.Visible = aimbotEnabled
end

local function getClosestPlayerToCursor()
    local closestPlayer = nil
    local shortestDistance = fovRadius

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= player and p.Character and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 and p.Character:FindFirstChild("HumanoidRootPart") then
            local pos, onScreen = Camera:WorldToViewportPoint(p.Character.HumanoidRootPart.Position)
            if onScreen then
                local cursorDistance = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)).Magnitude
                if cursorDistance < shortestDistance then
                    closestPlayer = p
                    shortestDistance = cursorDistance
                end
            end
        end
    end
    return closestPlayer
end

local function handleAimbot()
    if not aimbotEnabled then return end
    
    local target = nil
    if autoLock then
        target = getClosestPlayerToCursor()
    elseif aimOnHold and isHoldingAim then
        target = getClosestPlayerToCursor()
    end

    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
        local targetPos = target.Character.HumanoidRootPart.Position
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPos)
    end
end

-- Input Listeners cho Aimbot
UserInputService.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        isHoldingAim = true
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        isHoldingAim = false
    end
end)

-- Duy trì tốc độ & nhảy
local function handleMovement()
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        local hum = player.Character.Humanoid
        if speedEnabled then hum.WalkSpeed = walkSpeedValue else hum.WalkSpeed = 16 end
        if jumpEnabled then 
            hum.JumpPower = jumpPowerValue 
            hum.UseJumpPower = true
        else 
            hum.JumpPower = 50 
        end
    end
end

UserInputService.JumpRequest:Connect(function()
    if jumpEnabled and player.Character and player.Character:FindFirstChild("Humanoid") then
        player.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- ESP & Crosshair
local function updateCrosshair()
    if crosshairEnabled then
        local center = Camera.ViewportSize / 2
        local size = 10
        crosshairVertical.Visible = true
        crosshairVertical.From = Vector2.new(center.X, center.Y - size)
        crosshairVertical.To = Vector2.new(center.X, center.Y + size)
        crosshairVertical.Color = Color3.fromRGB(255, 0, 0)
        crosshairVertical.Thickness = 2
        crosshairHorizontal.Visible = true
        crosshairHorizontal.From = Vector2.new(center.X - size, center.Y)
        crosshairHorizontal.To = Vector2.new(center.X + size, center.Y)
        crosshairHorizontal.Color = Color3.fromRGB(255, 0, 0)
        crosshairHorizontal.Thickness = 2
    else
        crosshairVertical.Visible = false
        crosshairHorizontal.Visible = false
    end
end

-- =========================
-- 🎨 GIAO DIỆN (UI)
-- =========================

local CombatTab = Window:CreateTab("Chiến Đấu", 4483362458)

CombatTab:CreateSection("Hệ Thống Aimbot")
CombatTab:CreateToggle({
   Name = "KÍCH HOẠT AIMBOT (Enable Aimbot)",
   CurrentValue = false,
   Callback = function(Value) aimbotEnabled = Value updateFOV() end,
})
CombatTab:CreateToggle({
   Name = "Aim Khi Nhấn (Chuột Phải)",
   CurrentValue = false,
   Callback = function(Value) aimOnHold = Value end,
})
CombatTab:CreateToggle({
   Name = "Tự Động Aim (Auto Lock)",
   CurrentValue = false,
   Callback = function(Value) autoLock = Value end,
})
CombatTab:CreateSlider({
   Name = "ESP FOV Radius (Phạm vi)",
   Range = {30, 500},
   Increment = 1,
   CurrentValue = 100,
   Callback = function(Value) fovRadius = Value updateFOV() end,
})

CombatTab:CreateSection("Chế độ Tàn Sát")
CombatTab:CreateToggle({
   Name = "GIẾT TẤT CẢ (Kill Aura)",
   CurrentValue = false,
   Callback = function(Value) killAura = Value end,
})
CombatTab:CreateToggle({
   Name = "Auto Chat (Noob + Name)",
   CurrentValue = false,
   Callback = function(Value) autoChat = Value end,
})

CombatTab:CreateSection("Di Chuyển")
CombatTab:CreateToggle({Name = "BẬT CHẠY NHANH", CurrentValue = false, Callback = function(V) speedEnabled = V handleMovement() end})
CombatTab:CreateSlider({Name = "Tốc độ chạy", Range = {16, 200}, Increment = 1, CurrentValue = 16, Callback = function(V) walkSpeedValue = V handleMovement() end})
CombatTab:CreateToggle({Name = "BẬT NHẢY CAO", CurrentValue = false, Callback = function(V) jumpEnabled = V handleMovement() end})
CombatTab:CreateSlider({Name = "Lực nhảy", Range = {50, 500}, Increment = 1, CurrentValue = 50, Callback = function(V) jumpPowerValue = V handleMovement() end})

CombatTab:CreateSection("Hỗ trợ Ngắm")
CombatTab:CreateToggle({Name = "TÂM ẢO (Custom Crosshair)", CurrentValue = false, Callback = function(V) crosshairEnabled = V end})
CombatTab:CreateToggle({Name = "GIẢ CHẾT (Fake Death)", CurrentValue = false, Callback = function(V) fakeDeath = V end})

-- Tab Hiển Thị
local VisualTab = Window:CreateTab("Hiển Thị", 4483362458)
VisualTab:CreateSection("ESP Master Switch")
VisualTab:CreateToggle({Name = "HOẠT ĐỘNG ESP", CurrentValue = false, Callback = function(V) espMaster = V end})
VisualTab:CreateSection("Tùy chọn ESP")
VisualTab:CreateToggle({Name = "Hiện Box", CurrentValue = false, Callback = function(V) showBox = V end})
VisualTab:CreateToggle({Name = "Hiện Name", CurrentValue = false, Callback = function(V) showName = V end})
VisualTab:CreateToggle({Name = "Hiện Health", CurrentValue = false, Callback = function(V) showHealth = V end})

-- =========================
-- 🔁 VÒNG LẶP CHÍNH
-- =========================

-- Các hàm ESP (giữ nguyên từ bản V12) được gọi tại đây
local function createESP(p)
    local box = Drawing.new("Square")
    box.Visible = false
    box.Thickness = 1.5
    local nameTag = Drawing.new("Text")
    nameTag.Visible = false
    nameTag.Size = 14
    nameTag.Center = true
    espObjects[p] = {Box = box, NameTag = nameTag}
end

for _, p in pairs(Players:GetPlayers()) do if p ~= player then createESP(p) end end
Players.PlayerAdded:Connect(createESP)

RunService.RenderStepped:Connect(function()
    updateCrosshair()
    updateFOV()
    handleAimbot()
    handleMovement()
    
    -- Logic ESP cơ bản (thu gọn)
    for p, obj in pairs(espObjects) do
        if espMaster and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = p.Character.HumanoidRootPart
            local pos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
            if onScreen then
                if showBox then
                    obj.Box.Visible = true
                    obj.Box.Position = Vector2.new(pos.X - 25, pos.Y - 25)
                    obj.Box.Size = Vector2.new(50, 50)
                else obj.Box.Visible = false end
                if showName then
                    obj.NameTag.Visible = true
                    obj.NameTag.Text = p.Name
                    obj.NameTag.Position = Vector2.new(pos.X, pos.Y - 40)
                else obj.NameTag.Visible = false end
            else obj.Box.Visible = false obj.NameTag.Visible = false end
        else if obj then obj.Box.Visible = false obj.NameTag.Visible = false end end
    end
end)

-- Kill Aura logic (giữ nguyên từ bản V12)
RunService.Heartbeat:Connect(function()
    if killAura and not fakeDeath then
        if not currentTarget then
            local dist = math.huge
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character.Humanoid.Health > 0 then
                    local d = (player.Character.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
                    if d < dist then dist = d currentTarget = p end
                end
            end
        end
        if currentTarget and player.Character then
            player.Character.HumanoidRootPart.CFrame = currentTarget.Character.HumanoidRootPart.CFrame * CFrame.new(0, 2, 1)
            VirtualUser:Button1Down(Vector2.new(0,0))
            if currentTarget.Character.Humanoid.Health <= 0 then currentTarget = nil end
        end
    end
end)
